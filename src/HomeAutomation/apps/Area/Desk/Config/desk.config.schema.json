{
  "fields": {
    "light.longSensorDelaySeconds": { "type": "integer", "required": true, "min": 1, "max": 600 },
    "light.shortSensorDelaySeconds": { "type": "integer", "required": true, "min": 1, "max": 600 },
    "light.brightnessWhenSalaOn": { "type": "integer", "required": true, "min": 1, "max": 255 },
    "light.brightnessWhenSalaOff": { "type": "integer", "required": true, "min": 1, "max": 255 }
  },
  "comparisons": [
    {
      "leftPath": "light.longSensorDelaySeconds",
      "rightPath": "light.shortSensorDelaySeconds",
      "operator": "gte",
      "message": "Long sensor delay should be greater than or equal to short sensor delay."
    }
  ]
}
